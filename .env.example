# Local Authentication Example Configuration
# Copy this to .env and configure

# Data Directory (required)
DATA_DIRECTORY=/path/to/data

# FFmpeg (required)
FFMPEG_BINARY=/usr/bin/ffmpeg

# TMDB API (required for metadata)
TMDB_API_KEY=your_tmdb_api_key

# ============================================
# AUTHENTICATION CONFIGURATION
# ============================================

# Local Authentication (LiteDB-based)
# JWT_SECRET is required for authentication
JWT_SECRET=your-very-long-random-secret-key-at-least-32-characters-long
JWT_ISSUER=haas-media-local
JWT_AUDIENCE=haas-media-api
JWT_EXPIRATION_MINUTES=1440

# ============================================
# OTHER SETTINGS
# ============================================

# API Base URL
API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Internal API base URL used by the Next.js server (e.g. Docker network address)
INTERNAL_API_BASE_URL=http://downloader-api:8080

# CORS Origins
ALLOWED_CORS_ORIGINS=http://localhost:3000
